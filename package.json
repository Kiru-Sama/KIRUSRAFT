{
  "name": "kirusraft",
  "version": "0.0.1",
  "description": "KIRUSRAFT - 插件化跨平台 AI 工作台（TS 内核 + Cordis 插件系统）",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "esbuild src/index.ts --bundle --format=iife --outfile=www/kirusraft.js",
    "build": "npm run build:web",
    "build:apk": "npm run build:web && npx cap sync android && node scripts/ensure-proot-plugin.cjs",
    "test": "vitest run --pool=threads",
    "serve": "npx serve www"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.0",
    "@capacitor/preferences": "^8.0.1",
    "@deepseek-ai/cordis": "^4.0.1"
  },
  "devDependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "esbuild": "^0.28.0",
    "typescript": "^7.0.0",
    "vitest": "^4.1.11"
  }
}
