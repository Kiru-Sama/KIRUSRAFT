/**
 * KIRUSRAFT 版本号单一来源（v0.0.11）
 * 全项目版本号从这里读，禁止在其他文件硬编码版本号。
 * 升版本时运行 process 下的 bump 脚本，它会同步更新：
 *   - 本文件 VERSION
 *   - android/app/build.gradle 的 versionCode/versionName
 *   - capacitor.config.ts 的 version
 */
export const VERSION = '0.0.12';
